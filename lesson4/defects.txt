При вызове функции to_list с пустой строкой возвращается пустой массив, а должен вернуться список с одним элементом из пустой строки

Шаги:
1. Вызов функции to_list("", ",")

ОР - функция возвращает [""]
ФР - функция возвращает []